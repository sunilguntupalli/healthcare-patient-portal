spring:
  datasource:
    url: jdbc:h2:mem:careconnect;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate.ddl-auto: create-drop
    open-in-view: false
app:
  jwt-secret: test-secret-that-is-long-enough-for-hmac-signing-123456
  jwt-expiration-ms: 60000
  cors-origin: http://localhost:5174
  password-strength: 4
